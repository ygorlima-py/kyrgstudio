Configurar o R2
[FEITO]
Criar bucket privado, credenciais S3, endpoint e variáveis de ambiente locais e de produção.

Criar o adapter de storage
[FEITO]
Implementar R2Storage seguindo o contrato de storage atual, sem espalhar código específico do R2 pela aplicação.

Gerar URL pré-assinada
Criar endpoint autenticado que gera uma URL temporária para o frontend enviar o arquivo diretamente ao R2.

- contrato do storage
[FEITO]
Adicionar no S3Storage/R2Storage a geração da URLpré-assinada, sem alterar o upload local atual.

-Schemas públicos
[FEITO]
Criar os schemas da requisição e resposta, contendo apenas job_id, object_key, upload_url e expires_in.

-Endpoint autenticado
[FEITO]
Criar a rota que valida tipo, extensão e tamanho, cria o job pending e gera a URL temporária.

-Segurança e configuração
Garantir expiração curta, chave do objeto gerada pelo servidor, bucket privado e nenhuma credencial enviada ao frontend.

-Testes e validação
Testar usuário não autenticado, MIME inválido, arquivo grande, URL expirada, storage local e R2.
`

Confirmar o upload
[FEITO]
Criar endpoint que verifica o arquivo enviado, salva a referência no job e muda o status para uploaded.

Integrar API, fila e worker
[FEITO]
Fazer o worker baixar o arquivo do R2 temporariamente, processá-lo e remover o arquivo ao terminar.

Alterar o frontend
[feito]
Substituir o envio para a API pelo fluxo solicitar URL -> enviar ao R2 -> confirmar upload, mantendo progresso, cancelamento e mensagens de erro.

Segurança e validação
Configurar CORS, bucket privado, URLs com expiração, limpeza de arquivos e testar análise, adaptação, falhas e jobs concorrentes.